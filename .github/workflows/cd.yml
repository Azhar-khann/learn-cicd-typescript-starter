name: cd

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Deploy to production
        run: echo "Deploying to production..."

      - id: 'auth'
        uses: 'google-github-actions/auth@v2'
        with:
            credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v3'

      - name: 'Use gcloud CLI'
        run: 'gcloud info'

      - name: 'Build and push Docker image'
        run: 'gcloud builds submit --tag us-central1-docker.pkg.dev/project-2bf8c959-1a58-485f-a9f/notely-ar-repo/notely:latest .'
        
      - name: 'Run DB migrations'
        run: 'npm run db:migrate'
        
      - name: 'Deploy to Cloud Run'
        run: 'gcloud run deploy notely --image us-central1-docker.pkg.dev/project-2bf8c959-1a58-485f-a9f/notely-ar-repo/notely:latest --region us-central1 --allow-unauthenticated --project project-2bf8c959-1a58-485f-a9f --max-instances=4'
